export interface Planet {
  name: string;
  climate: string;
  gravity: string;
  population: string;
  created: Date;
  diameter?: string;
  rotation_period?: string;
  orbital_period?: string;
  terrain?: string;
  surface_water?: string;
}
